---
title: Example page
description: A sample standalone page showing the format. It is a draft, so it never appears on the website.
isDraft: true
---

This is a standalone page. It would be published at `/en/example-page/`.

Pages support full **markdown**: headings, lists, links, images uploaded through
the CMS, and so on.

To add the page to the header menu, link it from _Site settings → Header menu_
(e.g. `/example-page/`). Use the language switcher at the top of the
editor to write the Arabic and Finnish versions of the page — all translations
are edited together in this one entry, exactly like events.
